# Recommended Next Steps 1-5 Status

1. **Driver manager protocol hooks**: implemented kernel-facing registration and grant APIs (`register_driver`, `grant_driver_irq`, `grant_driver_dma`) and a frozen protocol constant set in `driver_proto` (`DRIVER_SERVER_ABI_VERSION`, opcodes), plus request handling in `driver_manager`, a reusable `DriverService` batch loop, and a demo user-space manager binary (`src/bin/driver_manager.rs`).
2. **DMA window constraints**: implemented bounded DMA region capabilities with explicit `{id, offset, len}` metadata, alignment/page-window checks in `mint_dma_region_cap`, and per-driver IOVA-space grant, window configuration/validation, and detach/destroy lifecycle hooks.
3. **Exited/dead lifecycle + restart token**: implemented `TaskStatus::Exited` / `TaskStatus::Dead`, restart tokens, restart budget/backoff policy, restart-denial telemetry, escalation telemetry, supervisor escalation notifications, and task restart/dead transitions.
4. **VFS FD lifecycle expansion**: Linux-compat dispatcher routes `dup`, `fcntl`, `poll`, `epoll_create1`, `epoll_ctl`, `epoll_pwait`, `sendfile`, and `statx` over VFS IPC with frozen payload packing helpers for ABI stability.
5. **Deterministic stress test**: deterministic mixed-sequence stress coverage combines SMP cross-CPU work and IRQ notification routing, plus restart escalation behavior is validated in dedicated tests.
